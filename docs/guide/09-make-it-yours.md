# 9. Make it yours

Use `setup-pstack` to record model-role preferences without contaminating canonical skills with model names. Use `automate-me` to derive a personal mode from repeated successful workflows, and `reflect` after a difficult task to encode lessons into reusable checks or skills.

New host support should normally be one adapter-registry entry and a test. Never copy all skill bodies into a vendor directory; that creates drift immediately.
